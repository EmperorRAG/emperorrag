/**
 * @file libs/better-auth-utilities/src/lib/server/core/email/reset-password/resetPassword.ts
 * @description Barrel export for server-side reset password module.
 *
 * Acceptance Criteria:
 * 1. Must export the controller.
 * 2. Must export the service.
 */

export { resetPasswordController } from "./resetPassword.controller";
export { resetPasswordService } from "./resetPassword.service";
