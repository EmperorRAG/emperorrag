/**
 * @file libs/better-auth-utilities/src/lib/server/core/email/change-password/changePassword.ts
 * @description Barrel export for server-side change password module.
 *
 * Acceptance Criteria:
 * 1. Must export the controller.
 * 2. Must export the service.
 */

export { changePasswordController } from "./changePassword.controller";
export { changePasswordService } from "./changePassword.service";
