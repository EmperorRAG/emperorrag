# tsserver-analyzer

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tsserver-analyzer` to build the library.

## Running unit tests

Run `nx test tsserver-analyzer` to execute the unit tests via [Vitest](https://vitest.dev/).
